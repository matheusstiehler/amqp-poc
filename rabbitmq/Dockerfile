# Base image with RabbitMQ (management plugin included for convenience)
FROM rabbitmq:3.9-management

# Expose standard RabbitMQ port 
EXPOSE 5672

# Expose management plugin port 
EXPOSE 15672 

# Optional: Copy a configuration file if you have one
# COPY rabbitmq.conf /etc/rabbitmq/rabbitmq.conf
